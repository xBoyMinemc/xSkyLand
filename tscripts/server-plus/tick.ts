// import EventSignal from "./EventSignal";
// import {system } from "@minecraft/server"
// import type { TickEvent } from "../@types/globalThis"

// const tick = new EventSignal<TickEvent>();


// const ticking = (event:TickEvent)=>(system.run(event=>ticking(event)),tick.trigger(event))

// system.run(()=>ticking({currentTick:system.currentTick}))


// export default tick
